{
  "name": "poke-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "license": "MIT",
  "description": "Poke at the app you're developing. A code buffer beside a live page."
}
